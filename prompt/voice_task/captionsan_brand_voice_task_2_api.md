# Brand Voice Task 2 — API
Add CRUD API endpoints for brand voice profiles. Support create, update, list, select default, and delete. Keep secrets and auth handling consistent with the current invite-only setup.
