# Prompt Template Task 1 — Schema
Add schema and data access for prompt templates. Each template should support name, description, category, target platforms, prompt body, placeholders, notes, favorite/default flags, and usage history. Keep it separate from projects and compatible with the prompt-first flow.
