# Task 1 — Define version history scope

Create the Version History Dashboard scope for CaptionSan.

Requirements:
- Separate prompt version history from content version history.
- Keep the feature compatible with the existing prompt-first workflow.
- Preserve invite-only auth, bilingual UI, and brand voice behavior.
- Identify the minimum data needed to show timeline, author, timestamp, status, and version label.

Acceptance criteria:
- The scope is clear and limited.
- Prompt and content version histories are explicitly separate.
- No unrelated feature changes are included.
